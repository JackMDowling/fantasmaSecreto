$(document).ready(() => {
  const $app = $("#app");
  $app.html("");

  const $title = $('<h2>Discussion</h2>');
  $title.appendTo($app);

  const $discussion = $('<div id=discussion>Discussion</div>')
});
